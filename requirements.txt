# ============================================
# JEFREY - Dependências de Produção
# Gerado a partir de pyproject.toml
# ============================================

# Core
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
pyyaml>=6.0,<7
python-dotenv>=1.0,<2

# LLM & Agent Framework
langchain-core>=0.2,<1
langchain-openai>=0.1,<1
langchain-anthropic>=0.1,<1
langchain-ollama>=0.1,<1
langgraph>=0.2,<1

# Memory & Embeddings
chromadb>=0.5,<1
langchain-chroma>=0.1,<1
tiktoken>=0.7,<1

# Database & Vector Store (Fase P1)
sqlalchemy>=2.0,<3
pgvector>=0.2,<1
psycopg[binary]>=3.1,<4
redis>=5.0,<6
alembic>=1.13,<2

# Web Search
tavily-python>=0.3,<1

# Audio (opcional - lazy load)
speechrecognition>=3.10,<4
pvporcupine>=3.0,<4

# Utilities
tenacity>=8.2,<9
rich>=13.7,<14
typer>=0.12,<1
httpx>=0.27,<1
orjson>=3.9,<4

# Fase 2 - OpenAI Agents SDK & Responses API + checkpointer Postgres
openai-agents>=0.1,<2
langgraph-checkpoint-postgres>=2,<4

# Observabilidade (logging estruturado JSON)
# Nota: o pacote no PyPI é 'python-json-logger' (fornece o módulo 'pythonjsonlogger').
# A pinagem antiga 'pythonjsonlogger<2' apontava para o nome descontinuado e quebrou o
# build do Docker em P3b (nenhuma versão disponível). Corrigido para o nome atual.
python-json-logger>=2,<5

# Fase 3 - MCP Gateway (servidor dedicado streamable-http)
# mcp 2.x renomeia FastMCP->MCPServer; openai-agents exige mcp<3,>=1.19.0
mcp>=1.19.0,<3

google-api-python-client>=2.0
google-auth-oauthlib>=1.1
google-auth>=2.0
duckduckgo-search>=6.0
ddgs>=9.0  # N1 rename fallback
langchain>=0.2.0
langchain-community>=0.2.0
asyncio-mqtt>=0.13.0
click>=8.0.0
uvicorn>=0.34.0
fastapi>=0.110.0
prometheus-client>=0.20.0
packaging>=24.0
cachetools>=5.3.0
PyJWT>=2.9.0
cryptography>=42.0.0

# Voice STT — faster-whisper (J.A.R.V.I.S. fix v1.6)
# HPP cap1-4 int8 cpu, lazy load in stt_engine.py
faster-whisper>=1.0.0
onnxruntime>=1.17.0
av>=10.0.0
